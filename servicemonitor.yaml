apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sensor-monitor
  labels:
    release: monitoring   
spec:
  selector:
    matchLabels:
      app: sensor-app
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: http
      path: /metrics
      interval: 5s